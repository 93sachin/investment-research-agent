{
  "name": "investment-research-agent",
  "version": "1.0.0",
  "scripts": {
    "postinstall": "npm install --prefix backend --legacy-peer-deps && npm install --prefix frontend",
    "start": "concurrently \"npm start --prefix backend\" \"npm run dev --prefix frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
